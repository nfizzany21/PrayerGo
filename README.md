# PrayerGo
An application to help user locate and find nearby mosque and prayer rooms. User can enter the new prayer location and the data linked to the database. Another system is admin website which admin can handle the database of prayer locations and can perform CRUD operations.
